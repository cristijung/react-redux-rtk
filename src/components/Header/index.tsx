import styles from './Header.module.scss'

export function Header() {
    return (
        <header className={styles.header}>
            <h1>Redux - Redux ToolKit</h1>
            <h2>Trabalhando com createSlice</h2>
        </header>

    );
    
}